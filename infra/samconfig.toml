version = 0.1

[default.deploy.parameters]
stack_name = "anviinnovate"
region = "us-east-1"
capabilities = "CAPABILITY_IAM"
resolve_s3 = true
confirm_changeset = false
# HostedZoneId is passed at deploy time (resolved from Route 53) by scripts/deploy.sh
# and the CI workflow. GoogleClientId/DomainName/SiteBucketName use template defaults.
